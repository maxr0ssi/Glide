import React from 'react';
import ReactDOM from 'react-dom/client';
import { GlideDemo } from './components/GlideDemo';
import './styles/glide-demo.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GlideDemo />
  </React.StrictMode>,
);
